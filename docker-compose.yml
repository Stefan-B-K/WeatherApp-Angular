version: "3.9"
services:
       webapp:
              build:
                     context: .
                     dockerfile: Dockerfile
              volumes:
                     - /app/node_modules
                     - .:/app
volumes:
       webapp:
              driver: local
